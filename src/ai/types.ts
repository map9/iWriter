/**
 * @deprecated Import shared AI contracts from `@shared/ai/contracts` in new code.
 * This renderer entry remains temporarily while existing consumers migrate.
 */
export * from '../../shared/ai/contracts'
