diff --git a/node_modules/@langchain/openai/dist/converters/responses.cjs b/node_modules/@langchain/openai/dist/converters/responses.cjs
index 3a87dc4..925f314 100644
--- a/node_modules/@langchain/openai/dist/converters/responses.cjs
+++ b/node_modules/@langchain/openai/dist/converters/responses.cjs
@@ -753,13 +753,9 @@ const convertStandardContentMessageToResponsesInput = (message) => {
 			}];
 			const reasoningItem = {
 				type: "reasoning",
-				id: block.id ?? "",
+				...(block.id ? { id: block.id } : {}),
 				summary
 			};
-			if (block.reasoning) reasoningItem.content = [{
-				type: "reasoning_text",
-				text: block.reasoning
-			}];
 			return reasoningItem;
 		};
 		const convertFunctionCall = (block) => ({
@@ -784,7 +780,7 @@ const convertStandardContentMessageToResponsesInput = (message) => {
 				return block.extras.phase;
 			});
 			pushMessageContent([{
-				type: "input_text",
+				type: messageRole === "assistant" ? "output_text" : "input_text",
 				text: block.text
 			}], phase);
 		} else if (block.type === "invalid_tool_call") {} else if (block.type === "reasoning") {
diff --git a/node_modules/@langchain/openai/dist/converters/responses.js b/node_modules/@langchain/openai/dist/converters/responses.js
index 6480cac..2cac70a 100644
--- a/node_modules/@langchain/openai/dist/converters/responses.js
+++ b/node_modules/@langchain/openai/dist/converters/responses.js
@@ -753,13 +753,9 @@ const convertStandardContentMessageToResponsesInput = (message) => {
 			}];
 			const reasoningItem = {
 				type: "reasoning",
-				id: block.id ?? "",
+				...(block.id ? { id: block.id } : {}),
 				summary
 			};
-			if (block.reasoning) reasoningItem.content = [{
-				type: "reasoning_text",
-				text: block.reasoning
-			}];
 			return reasoningItem;
 		};
 		const convertFunctionCall = (block) => ({
@@ -784,7 +780,7 @@ const convertStandardContentMessageToResponsesInput = (message) => {
 				return block.extras.phase;
 			});
 			pushMessageContent([{
-				type: "input_text",
+				type: messageRole === "assistant" ? "output_text" : "input_text",
 				text: block.text
 			}], phase);
 		} else if (block.type === "invalid_tool_call") {} else if (block.type === "reasoning") {
